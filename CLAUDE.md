# Alley 작업 규칙

## 문서는 셋으로 나뉜다

| 문서 | 다루는 것 | 언제 고치나 |
| --- | --- | --- |
| `docs/design.md` | 무엇을 왜 이렇게 만드는가 | 설계가 바뀔 때 |
| `docs/implementation-plan.md` | 무엇을 언제 만드는가 | 작업을 마칠 때마다 |
| `docs/adr/` | 개별 결정의 배경과 버려진 대안 | 결정할 때 (아래 참조) |

설계 문서에 일정이나 체크리스트를 넣지 않는다. 계획 문서에 왜 그렇게 정했는지를
길게 쓰지 않는다. 섞이면 둘 다 낡는다.

작업을 마치면 `docs/implementation-plan.md`의 해당 항목을 체크하고 진행 현황
표를 갱신한다.

### 용어집은 결정을 담지 않는다

`docs/glossary.md`는 위 셋과 성격이 다른 참조 문서다. 서버 쪽 용어를 모르는
사람이 나머지 문서를 읽을 수 있게 하는 것이 목적이다.

- **이 레포에 실제로 나오는 용어만** 넣는다. 일반 백과사전이 되면 아무도 안 고친다
- 교과서적 정의가 아니라 **"Alley에서 이것이 무엇을 하는가"** 를 쓴다.
  그래야 코드가 바뀔 때 같이 고칠 이유가 생긴다
- 왜 그렇게 정했는지는 쓰지 않는다. 그건 ADR 몫이고, 링크만 건다

## 아키텍처 결정은 ADR로 남긴다

되돌리기 어렵거나 대안을 놓고 고민한 결정을 내렸다면, 그 작업을 끝내기 전에
`docs/adr/`에 ADR을 추가한다. 나중에 몰아서 쓰지 않는다. 왜 그렇게 정했는지와
무엇을 버렸는지는 시간이 지나면 재구성할 수 없다.

기준과 형식은 [docs/adr/README.md](docs/adr/README.md)를 따른다. 요약하면:

- 언어·프레임워크·배포 형태, 보안·신뢰 경계, 그럴듯한 대안을 놓고 고른 경우에 쓴다
- 관례를 따른 선택이나 언제든 되돌릴 수 있는 구현 세부에는 쓰지 않는다
- **대안의 장점을 먼저 인정하고** 기각 이유를 쓴다
- **나쁜 점을 반드시 쓴다.** 없다고 쓰면 검토가 부족했다는 뜻이다
- 결정이 바뀌면 기존 ADR을 고치지 않고 새로 쓴 뒤 기존 것을 `대체됨`으로 바꾼다

설계 문서(`docs/design.md`)와 ADR이 어긋나면 ADR이 최신이다. 설계 문서도 함께
갱신한다.

## 조직 고유값을 코드에 넣지 않는다

이 레포는 오픈소스 공개를 전제로 한다 (ADR-0003). 특정 조직의 이름, 내부 호스트,
실제 계정 주소는 코드·주석·문서 어디에도 넣지 않는다. 예시가 필요하면
`example.com`, `com.example` 계열을 쓴다.

커밋 전에 확인한다:

```bash
DENYLIST_TERMS="조직명,조직약어" ./scripts/check-denylist.sh
```

## 셸에서 `cat`을 쓰지 않는다

이 환경의 셸에서 `cat`이 별칭돼 있다.

```
cat is an alias for bat --paging=always --color=always --line-range=:500
```

두 가지 문제가 있고, 둘 다 조용히 잘못된 결과를 만든다.

**1. `--color=always`가 리다이렉트에도 ANSI를 섞는다.**

```bash
cat > file.txt <<'EOF'    # 하지 말 것
내용
EOF
```

파일에 내용 대신 ANSI 색상 코드와 박스 그리기 문자가 들어간다.
`$(cat <<'EOF' ... EOF)`로 문자열을 만들 때도 마찬가지다. 커밋 메시지뿐 아니라
설정 파일, 다이어그램 소스, 스크립트 등 heredoc으로 만드는 모든 파일이 오염된다.
게다가 만든 파일을 다시 `cat`으로 확인하면 정상처럼 보여서 알아채기 어렵다.

**2. `--line-range=:500`이 500줄에서 말없이 자른다.**

600줄 파일을 읽으면 앞의 500줄만 나온다. 경고도 없다. 긴 파일을 읽고 "이게
전부"라고 판단하면 틀린다.

### 그래서

- 파일을 만들 때는 편집 도구를 쓴다 (Write/Edit)
- 파일을 읽을 때도 편집 도구를 쓴다. `grep`으로 찾는 것도 안전하다
- 커밋 메시지는 파일에 쓰고 `git commit -F <file>`
- 파이프라인 안에서 진짜 `cat`이 꼭 필요하면 `command cat`으로 별칭을 우회한다

## 커밋

- 태스크 단위로 나눈다. 여러 관심사를 한 커밋에 섞지 않는다
- 본문에 **왜** 그렇게 했는지를 쓴다. 무엇을 바꿨는지는 diff가 말해준다
- 메시지는 파일에 쓰고 `git commit -F`를 쓴다 (위 heredoc 항목 참조)

## 검증

완료를 보고하기 전에 실제로 돌려본다.

```bash
swift build
swift test
```

백그라운드 실행 결과는 종료 코드만 믿지 말고 출력을 확인한다. 빈 출력에 exit 0이
나오는 경우가 있었다.
