# 여기에 CI 가 `alley-store-app-unsigned.zip` 을 넣습니다 (ADR-0048).
#
# 레포에는 두지 않습니다. macOS 에서 컴파일한 바이너리라 소스가 아니고, 커밋마다
# 500KB 가 쌓입니다. 이미지를 만들 때 같은 릴리스의 macOS 잡이 만든 것을 받아
# 넣습니다.
#
# 이 디렉터리가 비어 있으면 서버는 예전처럼 "번들이 없습니다" 로 굴러갑니다.
# 로컬에서 `docker build` 를 그냥 돌렸을 때가 그렇습니다.
