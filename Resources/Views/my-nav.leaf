#if(count(page.myTabs) > 0):
<nav class="tabs" aria-label="내 설정">
    #for(tab in page.myTabs):
    #if(tab.isCurrent):
    <span class="tab tab-current" aria-current="page">#(tab.title)</span>
    #else:
    <a class="tab" href="#(tab.path)">#(tab.title)</a>
    #endif
    #endfor
</nav>
#endif
