/// 이 제품 릴리스의 버전. **여기가 유일한 진실이다** (ADR-0043).
///
/// 운영 레포가 고정하는 값이고, 릴리스 태그(`v` 를 붙인 이 값)가 이것을 가리킨다.
/// 릴리스 워크플로가 태그와 이 상수를 대조해, 어긋나면 릴리스를 만들지 않는다.
///
/// **워커 버전(`WorkerVersion`)과 다른 값이다.** 워커는 스스로 갈아끼우면서 자기
/// 버전을 서버 것과 견주므로, 워커가 바뀌지 않은 릴리스에서 버전이 오르면 모든
/// 워커가 쓸데없이 자기를 갈아끼운다. 그래서 따로 둔다.
public enum AlleyVersion {
    public static let current = "0.6.2"
}
